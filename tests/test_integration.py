"""Retired: v0.4 SmartGrammar-based integration test.

The v0.5 replacement is in ``tests/test_core.py`` which exercises the full
TrimTab pipeline (put_many → generate → search) over the same in-memory DB.
"""
